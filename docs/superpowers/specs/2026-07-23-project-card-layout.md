# Projects 卡片排版统一

## 目标

- Songbang 仅显示中文标题并使用 💼 图标。
- 项目说明最多两行，且两张同一行卡片高度一致。
- 标题字号大于说明字号，并整体略微增大。

## 实现与验收

- 删除 Songbang 的 `titleEn` 与本地 SVG 图片字段，使用 `icon: "💼"`。
- 网格与卡片使用拉伸高度；说明使用两行截断并保留两行空间。
- 项目标题为 18px，说明为 16px。
- 保留未来双语项目可用的字体结构，不改变卡片跳转。
- 仅本地提交，不推送个人网站。
