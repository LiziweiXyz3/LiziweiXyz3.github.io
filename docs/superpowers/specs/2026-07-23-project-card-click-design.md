# Projects 卡片整卡跳转

## 目标

- 项目有 `link` 时，点击整张卡片在新标签页打开对应 GitHub 仓库。
- 移除卡片内单独的 `VIEW` 链接。
- 没有 `link` 的项目继续显示为非可点击卡片。

## 实现与验收

- `renderProjects()` 为有链接的项目生成带 `target="_blank"` 与 `rel="noopener"` 的链接卡片。
- 链接卡片保持既有卡片样式，并能使用键盘 Tab 聚焦。
- 验证渲染代码不再输出 `VIEW`，并保留新标签页安全属性。
- 仅在本地提交，不推送个人网站仓库。
